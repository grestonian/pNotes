<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gurpreet.pnotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PNotesnfs">
        <activity android:name=".ActivityNoteDetail"
            android:theme="@style/Theme.PNotes"></activity>
        <activity
            android:name=".ActivityAddNote"
            android:theme="@style/Theme.PNotes" />
        <activity
            android:name=".ActivitySplash"
            android:theme="@style/Theme.PNotes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityDashboard" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.PNotes" />
    </application>

</manifest>